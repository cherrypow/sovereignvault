name: sovereign_vault_x
description: A local-only, offline-first tracker for software licenses and keys — built on the Sovereign Vault engine.
publish_to: 'none'
version: 0.1.0-beta

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sovereign_core:
    path: ../sovereign_core
  intl: ^0.19.0
  uuid: ^4.4.2
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
